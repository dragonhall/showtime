= form_for @channel, html: { class: 'simple_form' } do |f|
  = f.input :name
  = f.input :icon, as: :file
  = f.input :logo, as: :file
  = f.input :domain
  = f.input :stream_path

  
  .actions
    = f.submit 'Save'
